<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cs.dit.mapper.UserMapper">

	<insert id="signup" parameterType="cs.dit.domain.UserVO"
		useGeneratedKeys="true" keyProperty="number">
		insert into user (id, pwd, email,
		nickname) values (#{id}, #{pwd}, #{email}, #{nickname})
	</insert>

	<select id="login" parameterType="hashMap"
		resultType="cs.dit.domain.UserVO">
		SELECT ID, NICKNAME FROM USER WHERE ID=#{arg0} AND PWD=#{arg1}
	</select>

	<update id="modify" parameterType="cs.dit.domain.UserVO" useGeneratedKeys="true" keyProperty="number">
		UPDATE USER SET id = #{id}, pwd = #{pwd}, email = #{email}, nickname = #{nickname}
		WHERE number = #{number}
	</update>

	<select id="get" resultType="cs.dit.domain.UserVO">
		SELECT * FROM USER WHERE nickname = #{nickname}
	</select>

	<select id="id_get" resultType="boolean">
    	SELECT COUNT(*) FROM USER WHERE id = #{id}
	</select>
	
	<select id="nickname_get" resultType="boolean">
    	SELECT COUNT(*) FROM USER WHERE nickname = #{nickname}
	</select>
	
	<delete id="remove" parameterType="cs.dit.domain.ReplyVO">
		DELETE FROM USER WHERE number = #{number}
	</delete>


</mapper>